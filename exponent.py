# -*- coding: utf-8 -*-

base = float(input('Enter the base: '))
exponent = float(input('Enter the exponent: '))

# evaluate base to the power of exponent
result = base ** exponent

print('{} to the power of {} is: {}'.format(base, exponent, result))
